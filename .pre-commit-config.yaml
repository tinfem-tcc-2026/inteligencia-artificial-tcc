repos:
  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v4.0.0-alpha.8
    hooks:
      - id: prettier
        types_with_patch: [markdown]
        files: ^src/
        args: ["--prose-wrap", "always", "--print-width", "100"]
